
@model libraryManagementSystem.Models.Entity.publishers

@{
    ViewBag.Title = "addPublisher";
    Layout = "~/Views/Shared/Layout_1.cshtml";
}

@using (@Html.BeginForm("addPublisher", "Publisher", FormMethod.Post))
{
    <div style="margin-top:15px;">

        <label>Publisher Name</label>
        @Html.TextBoxFor(p => p.publisher_name, new { @class = "form-control", autocomplete = "off" })
        @Html.ValidationMessageFor(p => p.publisher_name, "", new { @style = "color:red" })

    </div>

    <div style="margin-top:15px">
        <button class="btn btn-info" id="pubAddBtn">Add</button>
    </div>
}

<!--<form class="form-group" method="post">
    <div style="margin-top:15px">
        <label>
            Publisher Name
        </label>
        <input type="text" class="form-control" name="publisher_name" autocomplete="off"/>-->
@*note that name of the input text and category class' attribute category_name must be same*@
<!--</div>
    <div style="margin-top:15px">
        <button class="btn btn-info" id="pubAddBtn">Add</button>
    </div>
</form>-->
